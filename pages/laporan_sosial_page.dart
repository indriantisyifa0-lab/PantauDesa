import 'package:flutter/material.dart';
import '../core/form_laporan_template.dart';

class LaporanSosialPage extends StatelessWidget {
  const LaporanSosialPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const FormLaporanTemplate(
      title: 'Form Laporan Sosial',
    );
  }
}
